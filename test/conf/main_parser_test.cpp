#include <gtest/gtest.h>
#include "conf.hpp"

namespace test
{
void	init_config(const std::string &path)
{
	config::init_config(path);
}
} // namespace test


TEST(mainParserTest, )
{
	const char	*filepath = "";
}