events {
	worker_connections 1024;
	# use poll;
}

http {
	error_log /home/students/webserv/logs/error.log;

	server {
		# server_name students.42.jp;
		listen 0.0.0.0:3001;
		root /home/student/webserv/html;
		index index.html;
	}
}
