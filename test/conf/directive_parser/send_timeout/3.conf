events {}

http {
	keepalive_timeout 2ms;
	server {
		keepalive_timeout 2s;
		location / {
			keepalive_timeout 2;
		}
		location /min {
			keepalive_timeout 2m;
		}
		location /path {
			keepalive_timeout 2h;
		}
		location /tmp {
			keepalive_timeout 2d;
		}
	}
}