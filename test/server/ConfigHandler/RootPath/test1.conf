events {}

http {
  #root html;
  root /var/www/html;
  server {
    root /var/www/html/server1;

    location /root/ {
      root /var/www/root;
    }

    location /image/ {
      alias /var/www/data/;
    }

    location /no-root/ {
    }
  }
  server {
    server_name "second_server";
  }
}
